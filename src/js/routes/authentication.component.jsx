import React from "react";
import SignInForm from "../auth-forms/sign-in-form.component";

const Authentication = () => {
  return (
    <div className="authentication-container">
      <SignInForm />
    </div>
  );
};

export default Authentication;
